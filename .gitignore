# ─────────────────────────────────────────────────────────────
# 白名单式忽略：除下方放行项之外，顶层其余内容一律不进仓库。
# 本工作区还存放着样例模型、第三方技能库与个人文件，白名单比黑名单稳妥。
# ─────────────────────────────────────────────────────────────
/*
!/.gitignore
!/README.md
!/requirements.txt
!/server.py
!/agent/
!/webdemo/

# `_` 前缀 = 本机私有，不入仓库
# （含 agent/_local_key.py 的明文密钥、调试脚本、记忆库）
# 注意：`_*` 会连 __init__.py 一起吃掉，所以下面把双下划线的放回来。
_*
!__*

# Python 编译缓存：.pyc 会把字符串字面量编进去，密钥会跟着泄
__pycache__/
*.pyc
*.pyo

# 本机记忆库与运行时产物
*.db
*.db-wal
*.db-shm
outputs/

# 模型池：运行时会被界面「添加模型」改写，用户填的密钥会落进这个文件。
# 删掉不影响使用 —— server.py 首次启动会自动重新落盘三个预置项。
agent/custom_models.json

# 系统 / 编辑器
.vscode/
.idea/
Thumbs.db
desktop.ini
*.tmp
.DS_Store
